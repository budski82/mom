{
  "name": "moms-preview",
  "version": "1.0.0",
  "description": "establish a link to display event message",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "concurrently npm:watch:*",
    "watch:sync": "browser-sync start --server --no-notify --files 'html/*.html', 'htm/*.htm', 'css/*.css'",
    "watch:sass": "sass --watch src/scss/main.scss:public/css/main.css",
    "watch:mon": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0",
    "sass": "^1.77.1"
  }
}
